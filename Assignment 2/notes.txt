maxagant:

    minimax(state)
            return max(min-value(state)[0] - min-value(state)[1]))

    min-value(state):
        if state.terminal or cutoff:
            evaluate(state)
        else:
        v <- -infinity
            for each next_state in successor(state,"MAX"):
                v = min(v,max-value(next_state)

    max-value(state):
         if state.terminal or cutoff:
            evaluate(state)
        else:
        v <- infinity
            for each next_state in successor(state,"MIN"):
                v = min(min-value(next_state)



